import {
  DEFAULT_MIRROR_NODE_URLS,
  getNetworkUrl,
  getRequiredNetworkUrl,
  isDefaultNetwork,
} from "../mirror/provider";

export type { NetworkType } from "../mirror/provider";
export { DEFAULT_MIRROR_NODE_URLS, getNetworkUrl, getRequiredNetworkUrl, isDefaultNetwork };
