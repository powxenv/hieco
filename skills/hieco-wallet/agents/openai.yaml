interface:
  display_name: "Hieco Wallet"
  short_description: "Use the Hieco wallet runtime and React layer"
  default_prompt: "Use $hieco-wallet to build or explain integrations with @hieco/wallet or @hieco/wallet-react."

policy:
  allow_implicit_invocation: true
